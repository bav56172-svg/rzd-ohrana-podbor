BOT_TOKEN=вставь_токен_от_Master_Bot_сюда

# Куда слать Excel с результатами кандидатов после каждого пройденного теста.
# Пока не задано — рассылка пропускается (не ошибка), результат остаётся
# только в bot/data/candidates.xlsx.
EMAIL_TO=

# SMTP-доступ для отправки (нужен, только если EMAIL_TO задан)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
